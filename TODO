TODO

# Refactoring

• DONE: Rename “Activity” into “State” to prepare for GameKit migration
• DONE: Delete `Asylia.hpp` and use headers when they’re needed
• DONE: Replace `NULL` by `nullptr`
• DONE: Use `sol2` instead of `SLB`
• DONE: Replace `Interface` by new functions in `ResourceHelper`
• DONE: Rework the rendering engine to make it compatible with GameKit
• DONE: `Image` and `Sprite` should load textures from `gk::ResourceHandler`
• TODO: Remove useless dynamic allocations
• TODO: Move to `GameKit`
   ◦ DONE: ApplicationStateStack
   ◦ DONE: Input system
   ◦ DONE: ResourceHandler
   ◦ DONE: Audio system
   ◦ WIP: Rendering system
   ◦ TODO: Debug / Exception
   ◦ TODO: GameClock

• TODO: Move `Singleton` class into GameKit and use it in relevant places

# Refactoring issues

• TODO: Fix armor selection screen (items get deleted on selection)
• TODO: Fix map reset (the map won’t be reset if the player exits and comes back)
• TODO: Use events instead of a timer for key handling in `SelectableWindow`

